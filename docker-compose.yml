version: "3.9"

services:
  server:
    build: ./server
    container_name: surgeai-backend
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./client
    container_name: surgeai-frontend
    ports:
      - "3000:3000"
    depends_on:
      - server
    restart: always
